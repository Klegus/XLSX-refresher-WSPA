# Bandit configuration (CI blocks on medium+ severity findings)
# B101 assert (tests), B104 bind 0.0.0.0 (inside containers), B311 random (non-crypto use),
# B603 subprocess without shell
exclude_dirs: ['tests', 'venv', '.venv']
skips: ['B101', 'B104', 'B311', 'B603']
